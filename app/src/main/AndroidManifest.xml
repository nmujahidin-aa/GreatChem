<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_favicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_favicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreatChem"
        android:name=".App"
        tools:targetApi="31">

        <!--Letakkan Activity pertama yang akan dieksekusi isini-->
        <activity

            android:name=".presentation.splash.SplashActivity"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/Theme.GreatChem"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.auth.AuthActivity" />

        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="false" />

    </application>



</manifest>